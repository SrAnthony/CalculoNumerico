<%= form_with(model: function, local: true) do |form| %>
  <% if function.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(function.errors.count, "error") %> prohibited this function from being saved:</h2>

      <ul>
      <% function.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :expression %>
    <%= form.text_field :expression %>
  </div>

  <div class="field">
    <%= form.label :point_a %>
    <%= form.text_field :point_a %>
  </div>

  <div class="field">
    <%= form.label :point_b %>
    <%= form.text_field :point_b %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
